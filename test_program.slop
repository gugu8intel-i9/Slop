fn fib(n: int) -> int {
    if (n <= 1) {
        return n
    }
    return (fib((n - 1)) + fib((n - 2)))
}

fn main(args: array[string]) -> void {
    let let_val = 10
    let result = fib(let_val)
    print("Fibonacci of 10 is:")
    print(result)
}

